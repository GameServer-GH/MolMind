profile_id: competition_masld
display_name: MASLD 低毒降脂 · 默认能力集
plugins:
  molmind-core:
    enabled: true
    builtin: true
skills:
  masld_nominate: { enabled: true }
  masld_mechanism: { enabled: true }
  masld_explain: { enabled: true }
  masld_export_bundle: { enabled: true }
  masld_full_submission: { enabled: true }
policy:
  allow_live_default: false
  claim_ceiling: proxy_priority_only
  hitl_required: [apply_review, allow_live]
  catalog_opt_in_only: true
budgets:
  max_iterations: 4
  max_steps: 12
  max_tool_calls: 8
  max_retries_per_tool: 1
  max_wall_time_sec: 180
  max_observation_chars: 12000
  max_no_progress_rounds: 2
