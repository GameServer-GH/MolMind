# Catalog 总览（工程索引；产品价值见可扩展方案附录 C）
#
# 规则：
# - 本目录下所有 YAML：`builtin: false`，运行时以会话 opt-in 为准
# - 禁止写入 competition_masld / minimal 的默认 enabled
# - lab_extensible 仅允许添加，不预置启用
#
# 赛期可添加（有适配器）：
#   origene-mcp.yaml  → plugins/origene_mcp/
#   aurobind.yaml     → plugins/aurobind/
#
# 赛后高价值（空壳/清单）：
#   vcworld.yaml · enzyme-cage.yaml · eva-rna.yaml
#
# 扩展样例：
#   见 plugins/_template/README.md
